<%= form_for(@cage) do |f| %>
  <% if @cage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cage.errors.count, "error") %> prohibited this cage from being saved:</h2>

      <ul>
      <% @cage.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :max_capacity %><br>
    <%= f.number_field :max_capacity %>
  </div>
  <div class="field">
    <%= f.label :power %><br>
    <%= f.check_box :power %>
  </div>
  <div class="field">
    <%= f.label :contained %><br>
    <%= f.number_field :contained %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
